import styled from 'styled-components'

const Refresh = styled.button`
  border-radius: 40px;
  width: 40px;
  height: 40px;
  border: none;
  cursor: pointer;

  &:before {
    content: '';
  }
`
export default Refresh